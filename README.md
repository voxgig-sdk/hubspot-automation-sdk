# HubspotAutomation SDK

HubSpot Automation API client, generated from the OpenAPI spec.

HubSpot Automation API — merged from the vendor's per-API OpenAPI documents.

Learn more about HubSpot Automation API at [hubapi.com](https://hubapi.com).

This is an unofficial SDK for the HubSpot Automation public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/hubspot-automation-sdk/](https://voxgig-sdk.github.io/hubspot-automation-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **27 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new HubspotAutomationSDK()
const actionsv4collectionresponsepublicactiondefinitionforward = await client.ActionsV4CollectionResponsePublicActionDefinitionForward().load({ app_id: 1 })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = HubspotAutomationSDK.test({
  entity: {
    sequences_collection_response_with_total_public_sequence_lite: {
      test01: { id: 'test01' },
    },
  },
})
const sequencescollectionresponsewithtotalpublicsequencelites = await client.SequencesCollectionResponseWithTotalPublicSequenceLite().list()
// sequencescollectionresponsewithtotalpublicsequencelites is an array of SequencesCollectionResponseWithTotalPublicSequenceLite entities, populated with mock data
// — call sequencescollectionresponsewithtotalpublicsequencelites[0].data() for the record itself
console.log(sequencescollectionresponsewithtotalpublicsequencelites)
```

### Python

```python
client = HubspotAutomationSDK.test()
sequencescollectionresponsewithtotalpublicsequencelites = client.SequencesCollectionResponseWithTotalPublicSequenceLite().list()
print(sequencescollectionresponsewithtotalpublicsequencelites)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = HubspotAutomationSDK::test([
    "entity" => ["sequencescollectionresponsewithtotalpublicsequencelite" => ["test01" => []]],
]);
$sequencescollectionresponsewithtotalpublicsequencelites = $client->SequencesCollectionResponseWithTotalPublicSequenceLite()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.SequencesCollectionResponseWithTotalPublicSequenceLite(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:SequencesCollectionResponseWithTotalPublicSequenceLite():list()
```

### JavaScript

```js
const client = HubspotAutomationSDK.test()
const sequencescollectionresponsewithtotalpublicsequencelites = await client.SequencesCollectionResponseWithTotalPublicSequenceLite().list()
// sequencescollectionresponsewithtotalpublicsequencelites is an array of entities, populated with mock data
// — call sequencescollectionresponsewithtotalpublicsequencelites[0].data() for the record itself
console.log(sequencescollectionresponsewithtotalpublicsequencelites)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/hubspot-automation` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-automation-sdk/tags) |
| Python | `voxgig-sdk-hubspot-automation` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-automation-sdk/tags) |
| PHP | `voxgig-sdk/hubspot-automation` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-automation-sdk/tags) |
| Golang | `github.com/voxgig-sdk/hubspot-automation-sdk/go` | `go get github.com/voxgig-sdk/hubspot-automation-sdk/go@latest` |
| Lua | `voxgig-sdk-hubspot-automation` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-automation-sdk/tags) |
| JavaScript | `@voxgig-sdk/hubspot-automation-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-automation-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/hubspot-automation-sdk/go-cli` | `go install github.com/voxgig-sdk/hubspot-automation-sdk/go-cli/cmd/hubspot-automation@latest` |
| Go MCP server | `github.com/voxgig-sdk/hubspot-automation-sdk/go-mcp` | `go get github.com/voxgig-sdk/hubspot-automation-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { HubspotAutomationSDK } from '@voxgig-sdk/hubspot-automation'

const client = new HubspotAutomationSDK({
  apikey: process.env.HUBSPOT_AUTOMATION_APIKEY,
})


// Load a specific actionsv4collectionresponsepublicactiondefinitionforward (returns a ActionsV4CollectionResponsePublicActionDefinitionForward)
const actionsv4collectionresponsepublicactiondefinitionforward = await client.ActionsV4CollectionResponsePublicActionDefinitionForward().load({
  app_id: 1,
})
console.log(actionsv4collectionresponsepublicactiondefinitionforward)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o hubspot-automation-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "hubspot-automation": {
      "command": "/abs/path/to/hubspot-automation-mcp"
    }
  }
}
```

## Entities

The API exposes 27 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **ActionsV4CollectionResponsePublicActionDefinitionForward** | The ActionsV4CollectionResponsePublicActionDefinitionForward entity (load). | `/automation/actions/2026-09/{appId}` |
| **ActionsV4CollectionResponsePublicActionFunctionIdentifierNo** | The ActionsV4CollectionResponsePublicActionFunctionIdentifierNo entity (list). | `/automation/actions/2026-09/{appId}/{definitionId}/functions` |
| **ActionsV4CollectionResponsePublicActionRevisionForward** | The ActionsV4CollectionResponsePublicActionRevisionForward entity (list). | `/automation/actions/2026-09/{appId}/{definitionId}/revisions` |
| **ActionsV4PublicActionDefinition** | The ActionsV4PublicActionDefinition entity (create, load, update). | `/automation/actions/2026-09/{appId}/{definitionId}` |
| **ActionsV4PublicActionDefinitionRequiresObject** | The ActionsV4PublicActionDefinitionRequiresObject entity (load). | `/automation/actions/2026-09/{appId}/{definitionId}/requires-object` |
| **ActionsV4PublicActionFunction** | The ActionsV4PublicActionFunction entity (load). | `/automation/actions/2026-09/{appId}/{definitionId}/functions/{functionType}/{functionId}` |
| **ActionsV4PublicActionFunctionIdentifier** | The ActionsV4PublicActionFunctionIdentifier entity (update). | `/automation/actions/2026-09/{appId}/{definitionId}/functions/{functionType}/{functionId}` |
| **ActionsV4PublicActionRevision** | The ActionsV4PublicActionRevision entity (load). | `/automation/actions/2026-09/{appId}/{definitionId}/revisions/{revisionId}` |
| **AutomationV4ApiFlow** | The AutomationV4ApiFlow entity (create, load, update). | `/automation/v4/flows/{flowId}` |
| **AutomationV4BatchResponseApiFlow** | The AutomationV4BatchResponseApiFlow entity (create). | `/automation/v4/flows/batch/read` |
| **AutomationV4BatchResponseFlowIdWorkflowIdMapping** | The AutomationV4BatchResponseFlowIdWorkflowIdMapping entity (create). | `/automation/v4/workflow-id-mappings/batch/read` |
| **AutomationV4CollectionResponseApiFlowEmailCampaign** | The AutomationV4CollectionResponseApiFlowEmailCampaign entity (list). | `/automation/v4/flows/email-campaigns` |
| **AutomationV4CollectionResponseApiFlowListingForwardPaging** | The AutomationV4CollectionResponseApiFlowListingForwardPaging entity (list). | `/automation/v4/flows` |
| **AutomationV4CollectionResponseApiHistogramDataPointNo** | The AutomationV4CollectionResponseApiHistogramDataPointNo entity (load). | `/automation/v4/flows/performance/{flowId}` |
| **Basic** | The Basic entity (remove). | `/automation/sequences/2026-09/serviceaccounts/sequences/{sequenceId}` |
| **Callback** | The Callback entity (create). | `/automation/actions/callbacks/2026-09/{callbackId}/complete` |
| **Definition** | The Definition entity (create, remove). | `/automation/actions/2026-09/{appId}/{definitionId}/requires-object` |
| **EmailTemplatesCollectionResponsePublicFolderForwardPaging** | The EmailTemplatesCollectionResponsePublicFolderForwardPaging entity (list). | `/automation/email-templates/2026-09/folders` |
| **EmailTemplatesCollectionResponsePublicTemplateForwardPaging** | The EmailTemplatesCollectionResponsePublicTemplateForwardPaging entity (list). | `/automation/email-templates/2026-09` |
| **EmailTemplatesPublicTemplate** | The EmailTemplatesPublicTemplate entity (create, load, update). | `/automation/email-templates/2026-09/{templateId}` |
| **Function** | The Function entity (remove). | `/automation/actions/2026-09/{appId}/{definitionId}/functions/{functionType}/{functionId}` |
| **Sequence** | The Sequence entity (create, list, load, update). | `/automation/sequences/2026-09/serviceaccounts/sequences` |
| **SequencesCollectionResponseWithTotalPublicSequenceLite** | The SequencesCollectionResponseWithTotalPublicSequenceLite entity (list). | `/automation/sequences/2026-09` |
| **SequencesPublicSequence** | The SequencesPublicSequence entity (load). | `/automation/sequences/2026-09/{sequenceId}` |
| **SequencesPublicSequenceEnrollment** | The SequencesPublicSequenceEnrollment entity (load). | `/automation/sequences/2026-09/enrollments/contact/{contactId}` |
| **SequencesPublicSequenceEnrollmentLite** | The SequencesPublicSequenceEnrollmentLite entity (create). | `/automation/sequences/2026-09/enrollments` |
| **SequencesPublicSequencePerformance** | The SequencesPublicSequencePerformance entity (list). | `/automation/sequences/2026-09/serviceaccounts/sequences/{sequenceId}/performance` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from hubspotautomation_sdk import HubspotAutomationSDK

client = HubspotAutomationSDK({
    "apikey": os.environ.get("HUBSPOT_AUTOMATION_APIKEY"),
})


# Load a specific actionsv4collectionresponsepublicactiondefinitionforward (returns the record, raises on error)
actionsv4collectionresponsepublicactiondefinitionforward = client.ActionsV4CollectionResponsePublicActionDefinitionForward().load({"app_id": 1})
print(actionsv4collectionresponsepublicactiondefinitionforward)
```

### PHP

```php
<?php
require_once 'hubspotautomation_sdk.php';

$client = new HubspotAutomationSDK([
    "apikey" => getenv("HUBSPOT_AUTOMATION_APIKEY"),
]);


// Load a specific actionsv4collectionresponsepublicactiondefinitionforward (returns the ENTITY; call data_get() for the record; throws on error)
$actionsv4collectionresponsepublicactiondefinitionforward = $client->ActionsV4CollectionResponsePublicActionDefinitionForward()->load(["app_id" => 1]);
print_r($actionsv4collectionresponsepublicactiondefinitionforward->data_get());
```

### Golang

```go
import sdk "github.com/voxgig-sdk/hubspot-automation-sdk/go"

client := sdk.NewHubspotAutomationSDK(map[string]any{
    "apikey": os.Getenv("HUBSPOT_AUTOMATION_APIKEY"),
})


// Load a specific actionsv4collectionresponsepublicactiondefinitionforward
actionsV4CollectionResponsePublicActionDefinitionForward, err := client.ActionsV4CollectionResponsePublicActionDefinitionForward(nil).Load(
    map[string]any{"app_id": 1}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(actionsV4CollectionResponsePublicActionDefinitionForward)
```

### Lua

```lua
local sdk = require("hubspot-automation_sdk")

local client = sdk.new({
  apikey = os.getenv("HUBSPOT_AUTOMATION_APIKEY"),
})


-- Load a specific actionsv4collectionresponsepublicactiondefinitionforward
local actionsv4collectionresponsepublicactiondefinitionforward, err = client:ActionsV4CollectionResponsePublicActionDefinitionForward():load({ app_id = 1 })
print(actionsv4collectionresponsepublicactiondefinitionforward)
```

### JavaScript

```js
const { HubspotAutomationSDK } = require('@voxgig-sdk/hubspot-automation-js')

const client = new HubspotAutomationSDK({
  apikey: process.env.HUBSPOT_AUTOMATION_APIKEY,
})


// Load a specific actionsv4collectionresponsepublicactiondefinitionforward (returns the entity)
const actionsv4collectionresponsepublicactiondefinitionforward = await client.ActionsV4CollectionResponsePublicActionDefinitionForward().load({
  app_id: 1,
})
console.log(actionsv4collectionresponsepublicactiondefinitionforward)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.hubapi.com](https://api.hubapi.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the HubSpot Automation API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
